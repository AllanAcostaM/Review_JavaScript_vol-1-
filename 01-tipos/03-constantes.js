const nombre = "Hola Mundo";
nombre = "Chanchito feliz"

console.log(nombre);

/* Nota de recordatorio: Los valores de las variables generalmente no debrian ser
                         alterados, sino que cada variable deberia de contar con su propio valor
                         tal como en el ejemplo de aplicar "constantes" (const) en vez de "variables" (let o var)*/
                         