/* Notas: El orden en donde se ejecutan los operadores (matematicos, logicos o cual sea)
          tienen su propio orden de ejecucion, por ende si se necesita especificar
          un orden en donde se ejecute alguna evaluacion, se tiene que agregar un ´()´
          alrededor de la operacion que se quiera que se ejecute primero */


let resultado = 8/(2 * (2+2));
console.log(resultado);
